// apps/site/src/contracts/architecture.v1.gen.ts
// GENERATED from contracts/json/architecture.v1.schema.json by otsafety_tooling.contracts.zod.
// Do not edit: change the Pydantic model and run mise run contracts:generate.
import { z } from "zod";

export const architectureV1Schema = z.strictObject({ "contract": z.literal("architecture/v1"), "layers": z.array(z.strictObject({ "id": z.string().min(1), "n": z.string().regex(new RegExp("^[1-9]$")), "name": z.string().min(3), "origin": z.enum(["spec", "added"]), "question": z.string().min(20), "failure": z.string().min(40), "guard": z.string().min(3), "as_code": z.string().min(1), "as_data": z.string().min(3) }).describe("One layer of the model: what it answers, what fails, what guards it.")).min(9).max(9), "stages": z.array(z.strictObject({ "id": z.string().min(1), "title": z.string().min(3), "source": z.string().min(3), "as_code": z.string().min(3), "as_data": z.string().min(3), "gate": z.union([z.enum(["HALT", "REJECT"]), z.null()]) }).describe("One stage of the execution spine, and the gate it carries.")).min(1), "questions": z.array(z.strictObject({ "id": z.enum(["Q1", "Q2", "Q3"]), "title": z.string().min(10), "contrast": z.string().min(20), "if_it_ties": z.string().min(30) }).describe("One of the three nested questions, its contrast, and its null result.")).min(3).max(3), "rungs": z.array(z.strictObject({ "id": z.string().min(1), "name": z.string().min(3), "question": z.enum(["Q1", "Q2", "Q3"]), "detail": z.string().min(10) }).describe("One rung of the ladder, and which question it answers.")).min(1), "scope_in": z.array(z.string()).min(1), "scope_out": z.array(z.string()).min(1) }).describe("The architecture, stated once and read by the sheet, the site and the README.");
